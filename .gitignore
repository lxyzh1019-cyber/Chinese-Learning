# Live learner records exported by scripts/backup_players.js.
# These contain Jenn's and Jess's real progress — never commit them.
backups/

node_modules/
.DS_Store

# Claude Code working rules — tracked on purpose; only local hook state is ignored.
!.claude/
.claude/state/
__pycache__/
*.pyc
